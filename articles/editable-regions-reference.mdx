---
_schema: default
_uuid: b7e6c399-3b21-4b2b-af22-ce90aad355a6
_created_at: 2025-08-27T10:26:17+12:00
title: Editable Regions reference
nav_title: Editable Regions reference
published: true
image: >-
 https://cc-dam.imgix.net/documentation/images/default-thumbnail/2023-Q4/CloudCannon-Documentation-Thumbnail.png
description:
article_category:
 - Reference
article_topic:
author_notes:
 docshots_status:
---
x


<comp.DataReference>
  <comp.DataReferenceRow label="data-editable" type_markdown="Attibute, or component">
    This attribute defines the type of data inside an Editable.
    
    Value can be `array`, `array-item`, `component`, `image`, `source`, or `text`.

    Alternatively, you can wrap elements in the equivalent web component: `<array-editable>`, `<array-item>`, `<component-editable>`, `<image-editable>`, `<source-editable>`, or `<text-editable>`. If you use the web component, you do not need to define the `data-editable` attribute.

    <comp.Example>
      In this example, we added the `data-editable` attribute with the value `text` to the `<h1>` tag to indicate that the value is text data.

      <comp.CodeBlock language="HTML" source="index.html">
            ``````````
            <h1 data-editable="text" data-prop="title" >{title}</h1>

            ``````````
            </comp.CodeBlock>

      In this example, we have wrapped the `<slot />` element in the `<text-editable>` web component to indicate that the value is text data.

      <comp.CodeBlock language="HTML" source="blog-layout.html">
            ``````````
            <text-editable data-prop="@content">
              <slot />
            </text-editable>

            ``````````
            </comp.CodeBlock>
    </comp.Example>
  </comp.DataReferenceRow>
  <comp.DataReferenceRow label="data-prop" type_markdown="Attibute">
    This attribute defines the path type of data inside an Editable.
    
    Value can be `array`, `array-item`, `component`, `image`, `source`, or `text`.

    Alternatively, you can wrap elements in the equivalent web component: `<array-editable>`, `<array-item>`, `<component-editable>`, `<image-editable>`, `<source-editable>`, or `<text-editable>`. If you use the web component, you do not need to define the `data-editable` attribute.

    <comp.Example>
      In this example, we added the `data-prop` attribute with the value `text` to the `<h1>` tag to indicate that the value is text data.

      <comp.CodeBlock language="HTML" source="index.html">
            ``````````
            <h1 data-editable="text" data-prop="title" >{title}</h1>

            ``````````
            </comp.CodeBlock>

      In this example, we have wrapped the `<slot />` element in the `<text-editable>` web component to indicate that the value is text data.

      <comp.CodeBlock language="HTML" source="blog-layout.html">
            ``````````
            <text-editable data-prop="@content">
              <slot />
            </text-editable>

            ``````````
            </comp.CodeBlock>
    </comp.Example>
  </comp.DataReferenceRow>
  <comp.DataReferenceRow label="data-path" type_markdown="Attibute">
    x
    
    <comp.Example>
      In this example...

      <comp.CodeBlock language="HTML" source="index.html">
      ``````````
      x

      ``````````
      </comp.CodeBlock>
    </comp.Example>
  </comp.DataReferenceRow>
  <comp.DataReferenceRow label="data-key" type_markdown="Attibute">
    x
    
    <comp.Example>
      In this example...

      <comp.CodeBlock language="HTML" source="index.html">
      ``````````
      x

      ``````````
      </comp.CodeBlock>
    </comp.Example>
  </comp.DataReferenceRow>
  <comp.DataReferenceRow label="data-type" type_markdown="Attibute">
    x
    
    <comp.Example>
      In this example...

      <comp.CodeBlock language="HTML" source="index.html">
      ``````````
      x

      ``````````
      </comp.CodeBlock>
    </comp.Example>
  </comp.DataReferenceRow>
  <comp.DataReferenceRow label="data-id" type_markdown="Attibute">
    x
    
    <comp.Example>
      In this example...

      <comp.CodeBlock language="HTML" source="index.html">
      ``````````
      x

      ``````````
      </comp.CodeBlock>
    </comp.Example>
  </comp.DataReferenceRow>
  <comp.DataReferenceRow label="data-id-key" type_markdown="Attibute">
    x
    
    <comp.Example>
      In this example...

      <comp.CodeBlock language="HTML" source="index.html">
      ``````````
      x

      ``````````
      </comp.CodeBlock>
    </comp.Example>
  </comp.DataReferenceRow>
  <comp.DataReferenceRow label="data-component" type_markdown="Attibute">
    x
    
    <comp.Example>
      In this example...

      <comp.CodeBlock language="HTML" source="index.html">
      ``````````
      x

      ``````````
      </comp.CodeBlock>
    </comp.Example>
  </comp.DataReferenceRow>
</comp.DataReference>