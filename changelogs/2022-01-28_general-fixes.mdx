---
title: General fixes
type: minor
---
This release reduces syncing restrictions and fixes a couple issues.

**Features:**

* Increased `git clone` max time from 3 minutes to 9

**Fixes:**

* Unusable file operations hidden in editor locked sites
* Markdown configuration `hardWraps` now saves correctly with line breaks instead of break tags
