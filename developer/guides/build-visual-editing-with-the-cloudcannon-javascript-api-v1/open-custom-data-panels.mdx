---
_schema: default
_uuid: 5532d076-c178-4f64-99e4-fd07bb1a34d7
_created_at: '2026-03-12 02:32:58 +0000'
details:
  title: Open custom data panels
  order: 6
  image: >-
    https://cc-dam.imgix.net/documentation/images/guides/getting-started/guide-thumbnails/CloudCannon-Getting-Started-Thumbnail-Universal.png
  description: Use custom data panels to provide tailored editing controls.
  start_nav_group: Reactive behavior
  related_articles: null
author_notes:
  docshots: null
---
In this step, we'll open a custom data panel and apply updates to your UI.

1. Call `createCustomDataPanel()` with a title, data object, and config.
2. Handle changes in `onChange`.
3. Apply updated values to your preview.

<comp.CodeBlock language="javascript" source="src/custom-panel.js">
```
import { withCloudCannonApi } from './cloudcannon-api';

withCloudCannonApi(async (api) => {
  await api.createCustomDataPanel({
    title: 'Hero settings',
    data: {
      heading: 'Welcome',
      showCta: true
    },
    config: {
      _inputs: {
        heading: { type: 'text' },
        showCta: { type: 'boolean' }
      }
    },
    onChange: (updatedData) => {
      renderHeroPreview(updatedData);
    }
  });
});
```
</comp.CodeBlock>

Great work. You now have a complete end-to-end JavaScript API v1 workflow for initialization, content updates, collection reads, event handling, and custom editing UI.
