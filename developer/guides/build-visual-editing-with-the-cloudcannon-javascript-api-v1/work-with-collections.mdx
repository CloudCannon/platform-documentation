---
_schema: default
_uuid: db08cf2c-dc44-477f-a7c1-3fb79611e0b2
_created_at: '2026-03-12 02:32:58 +0000'
details:
  title: Work with collections
  order: 4
  image: >-
    https://cc-dam.imgix.net/documentation/images/guides/getting-started/guide-thumbnails/CloudCannon-Getting-Started-Thumbnail-Universal.png
  description: Load collection files and build simple collection filters.
  start_nav_group: Editing content
  related_articles: null
author_notes:
  docshots: null
---
In this step, we'll load a collection and filter results to drive custom UI.

1. Access a collection by key.
2. Call `items()` to get file objects.
3. Filter based on front matter values.

<comp.CodeBlock language="javascript" source="src/collection-updates.js">
```
import { withCloudCannonApi } from './cloudcannon-api';

withCloudCannonApi(async (api) => {
  const posts = api.collection('posts');
  const files = await posts.items();
  const published = [];

  for (const file of files) {
    const data = await file.data.get();
    if (data?.published) published.push(file.path);
  }

  console.log('Published post files:', published);
});
```
</comp.CodeBlock>

Next, we'll make your UI reactive with event listeners.
