#!/bin/bash
# Pagefind is now integrated into Lume via _config.ts plugin.
# This postbuild is kept as a fallback for CloudCannon builds.
# npx -y pagefind@1.5.0-beta.1 --site _site --output-subdir documentation/_pagefind